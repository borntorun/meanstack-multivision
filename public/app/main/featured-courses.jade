.panel.panel-primary
    .panel-heading.text-capitalize.text-center Featured courses
    .panel-body
        .row(ng-repeat="course in courses | filter: {featured:true}")
            .col-md-12 {{course.name}}